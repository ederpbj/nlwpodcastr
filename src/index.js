import React from 'react';
import ReactDOM from 'react-dom';
import App from './App';

ReactDOM.render(
    <App />, //mostra o app dentro do root, que é o projeto react no arquivo App.js, funções retornaod html
  document.getElementById('root') //vai no root e busca o elemento pelo id
);
